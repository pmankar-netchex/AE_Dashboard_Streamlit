# Salesforce Credentials
# Copy this file to .env and fill in your credentials
# Never commit .env to version control

# === OAuth (recommended, like n8n) ===
# Create a Connected App in Salesforce: Setup → App Manager → New Connected App
# Enable OAuth, set Callback URL to your app URL (e.g. http://localhost:8501)
# OAuth Scopes in Connected App:
#   Manage user data via APIs          → api
#   Perform requests at any time       → refresh_token / offline_access
SALESFORCE_CLIENT_ID=your_consumer_key
SALESFORCE_CLIENT_SECRET=your_consumer_secret
SALESFORCE_REDIRECT_URI=http://localhost:8501
SALESFORCE_SANDBOX=false

# Custom domain (optional) - use if your org has My Domain, e.g.:
# SALESFORCE_LOGIN_URL=https://netchex.my.salesforce-setup.com

# OAuth scopes (optional) - default matches Connected App scopes above:
# SALESFORCE_OAUTH_SCOPES="api refresh_token offline_access"
# If using Full access only: SALESFORCE_OAUTH_SCOPES="full refresh_token"

# === Username/Password (legacy) ===
# SALESFORCE_USERNAME=your_email@company.com
# SALESFORCE_PASSWORD=your_password
# SALESFORCE_SECURITY_TOKEN=your_security_token

# ====================================================================
# AZURE AD / MSAL AUTHENTICATION (Optional)
# ====================================================================
# Add these to enable Microsoft/Azure AD authentication for the dashboard
# Users will need to sign in with their Microsoft account before accessing

# === Azure AD App Registration Setup ===
# 1. Go to Azure Portal → Azure Active Directory → App registrations → New registration
# 2. Name: "AE Dashboard" or similar
# 3. Supported account types: Choose based on your needs (single tenant recommended)
# 4. Redirect URI: Web - http://localhost:8501 (or your deployment URL)
# 5. After creation, copy Application (client) ID and Directory (tenant) ID below
# 6. Create a client secret: Certificates & secrets → New client secret → Copy the value
# 7. Add API permissions: Microsoft Graph → Delegated permissions → User.Read → Grant admin consent

# Application (client) ID from Azure AD app registration
# AZURE_CLIENT_ID=your_azure_client_id_here

# Directory (tenant) ID from Azure AD
# AZURE_TENANT_ID=your_tenant_id_here

# Client secret value from Azure AD app registration
# AZURE_CLIENT_SECRET=your_azure_client_secret_here

# Redirect URI (must match what's configured in Azure AD)
# AZURE_REDIRECT_URI=http://localhost:8501

# Optional: Authority URL (defaults to https://login.microsoftonline.com/{tenant_id})
# AZURE_AUTHORITY=https://login.microsoftonline.com/your_tenant_id_here

# Optional: Space-separated scopes (defaults to "User.Read")
# AZURE_SCOPES="User.Read Mail.Read"

# Optional: Allowed email domains (comma-separated, e.g., "company.com,subsidiary.com")
# Users must have email addresses from these domains to access the dashboard
# AZURE_ALLOWED_DOMAINS=company.com

# Optional: Allowed email addresses (comma-separated)
# Only these specific users can access the dashboard
# AZURE_ALLOWED_EMAILS=admin@company.com,manager@company.com

# ====================================================================
# OPTIONAL SETTINGS
# ====================================================================
# DEBUG=1  # Enable debug mode (shows filter values)
