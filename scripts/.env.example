# Salesforce Credentials
# Copy this file to .env and fill in your credentials
# Never commit .env to version control

# === OAuth (recommended, like n8n) ===
# Create a Connected App in Salesforce: Setup → App Manager → New Connected App
# Enable OAuth, set Callback URL to your app URL (e.g. http://localhost:8501)
# OAuth Scopes in Connected App:
#   Manage user data via APIs          → api
#   Perform requests at any time       → refresh_token / offline_access
SALESFORCE_CLIENT_ID=your_consumer_key
SALESFORCE_CLIENT_SECRET=your_consumer_secret
SALESFORCE_REDIRECT_URI=http://localhost:8501
SALESFORCE_SANDBOX=false

# Custom domain (optional) - use if your org has My Domain, e.g.:
# SALESFORCE_LOGIN_URL=https://netchex.my.salesforce-setup.com

# OAuth scopes (optional) - default matches Connected App scopes above:
# SALESFORCE_OAUTH_SCOPES="api refresh_token offline_access"
# If using Full access only: SALESFORCE_OAUTH_SCOPES="full refresh_token"

# === Username/Password (legacy) ===
# SALESFORCE_USERNAME=your_email@company.com
# SALESFORCE_PASSWORD=your_password
# SALESFORCE_SECURITY_TOKEN=your_security_token
